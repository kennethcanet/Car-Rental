// Navigation is handled by expo-router via the src/app/ file-based routing system.
// This file is reserved for typed navigation helpers, shared route constants,
// and any imperative navigation utilities that features may need.
//
// Example usage (future):
// import { router } from "expo-router";
// export const navigateToBooking = (id: string) => router.push(`/(app)/bookings/${id}`);
